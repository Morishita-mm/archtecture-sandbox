# ==============================================================================
# Architecture Sandbox - Next Development Steps
# ==============================================================================
# This file outlines the roadmap for future features and improvements.
# ==============================================================================

# ------------------------------------------------------------------------------
# 🚀 Phase 1: High Priority (表現力と評価精度の向上)
# ------------------------------------------------------------------------------
phase_1_immediate:
  - feature: "グループ化ノード (Sub Flows) の実装"
    priority: "Highest"
    description: "VPC, Availability Zone (AZ), Subnet などの「包含関係」を表現するための機能。"
    implementation_plan:
      - React Flowの "Sub Flows" 機能を利用する。
      - 新しいノードタイプ `GroupNode` (枠のみのコンポーネント) を作成する。
      - ノードの `parentNode` 属性を利用して、どのAZ/VPCに属しているかを管理する。
    expected_impact:
      - "Multi-AZ (冗長化) 構成を視覚的に正しく表現できる。"
      - "AI評価時に「AZ分散ができているか」を正確に判定できるようになる。"

# ------------------------------------------------------------------------------
# ✨ Phase 2: UI/UX Enhancements (使い勝手とシェア機能の強化)
# ------------------------------------------------------------------------------
phase_2_ux_polish:
  - feature: "接続線 (Edge) の表現力強化"
    priority: "High"
    description: "コンポーネント間の通信プロトコルや性質を視覚化する。"
    details:
      - ラベル機能: `HTTPS`, `SQL`, `gRPC` などのテキストを線上に表示。
      - 線種の変更: 実線 (同期通信), 点線 (非同期/メッセージング) の切り替え。
      - 方向性: 双方向, 単方向の矢印制御。

  - feature: "設計図の画像エクスポート"
    priority: "Medium"
    description: "現在のキャンバスの状態を画像ファイル (PNG/JPG) としてダウンロードする機能。"
    tech_stack: "html-to-image or similar library"
    use_case: "SNSシェア時に、スコアだけでなく実際の構成図も添付できるようにする。"

# ------------------------------------------------------------------------------
# 📦 Phase 3: Content & Onboarding (コンテンツ拡充)
# ------------------------------------------------------------------------------
phase_3_content:
  - feature: "プリセット構成 (テンプレート) の配置"
    priority: "Low"
    description: "よくある構成パターンをワンクリックで一括配置する機能。"
    examples:
      - "Web 3層構成 (ALB + EC2 + RDS)"
      - "サーバーレス構成 (API Gateway + Lambda + DynamoDB)"
    target_user: "ゼロから配置するのが難しい初学者"

# ------------------------------------------------------------------------------
# 🏗️ Infrastructure & Operations (将来的な検討事項)
# ------------------------------------------------------------------------------
future_considerations:
  - topic: "CI/CD Pipeline"
    status: "On Hold"
    reason: "GitHub Actionsの課金コスト回避のため手動デプロイを継続。必要に応じて再検討。"
  
  - topic: "User Authentication"
    status: "Pending"
    solution: "Auth0 or Amazon Cognito"
    trigger: "個人の学習履歴管理や、クラウドへのプロジェクト保存が必要になった場合。"

  - topic: "Server-side Persistence"
    status: "Pending"
    solution: "Amazon DynamoDB (Serverless)"
    trigger: "ローカルJSON保存での運用に限界が来た場合。"

  - topic: "Automated Testing"
    status: "Recommended"
    action: "バックエンドの評価ロジック(Unit Test)と、主要なE2Eテストの導入を検討。"